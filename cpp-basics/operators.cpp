// this is vim btw.
#include <iostream>
#include <cmath>

using namespace std;

int main(){
	double a=5,b=10,t;
//	cout << a << endl;
//	t=a;
//	a=b;
//	b=t;
//
//	priority () >> * / >> + - , just like how in math.
//	t= (a + 10)/( 3 * b);
//	cout << t << endl;
	
	// with newtons 2nd law, f=ma, taking values
	//double mass, acceleration,force;
	//cout << "Enter Mass and Acceleration values:";
	//cin >> mass >> acceleration;
	//force = mass * acceleration;
	//cout << "force is:" << force << "N" << endl;
	

	// area of cicle:
	double radius=3, pi=3.1415, area;
	area = pi * (pow(3,2));
	cout << area << endl;


	//you can fix variables by adding a 'const' as prefix
	//uninitialized value will throw a random number, initialize with 0 or use {} while declaring;
	return 0;

}
